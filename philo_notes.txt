philo notes

The if (argc == 5) block is wrong
You check argc < 5 || argc > 6 first, then immediately after do if (argc == 5). That means if someone passes 6 arguments (valid!) your entire parsing block is skipped. Remove that wrapper — just always parse, and handle the optional 6th arg with its own check:

- sim->limit_meals = -1; //when 5 args this is not checked, set to -1 for later

- That sim you malloc'd is immediately leaked. Nothing in your program can use it.
>> malloc your structs in main and pass it to the fn parse_args so you can free later as well!!
or this...
:- Then parse_args takes t_sim_data *sim as a parameter — no malloc needed here. The big mallocs (forks array, philos array) happen in init_data in Phase 3.

✅ Correct number of arguments (4 or 5)
✅ All arguments are digits only (no letters, no symbols)
✅ Negative numbers caught with a specific message
✅ Empty string arguments caught
✅ Overflow above INT_MAX caught
✅ All values are positive (no zeros)
✅ limit_meals defaults to -1 when 5th arg not given
✅ print_error writes to stderr and exits cleanly
✅ sim is a stack variable in main, no leaked mallocs


parsing almost done, the values given as a parameter are given in milliseconds and usleep needs those values in MICROseconds.

time to think is not ever set because it has to be calculated in between run time always, time to die is how long the entire simulation runs for and each philosopher needs to eat at least twice within that time otherwise DEATH
check if time_to_eat, time_to_die, time_to_sleep is less than 60ms

parsing done for input, but not completely //come back to this

------------------
initialising data
------------------

create a fn that initilises the data at t_sim_data: 

typedef struct s_sim_data {
	long philo_n;
	long time_to_die;
	long time_to_eat;
	long time_to_sleep;
	long limit_meals; //5th arg if specified that will end sim at n
	long start_sim;
	int end_sim; //boolean whether all philos ate or one died
	t_fork *forks;
	t_philo *philos;
	 
}	t_sim_data;

specifically for the last 3
for allocating philos
	sim_data->philos = malloc(sizeof(t_philo) * sim_data->philo_n);
a malloc the size of t_philo (not t_sim_data) times the number saved in t_sim_data philo_n

-- handle mutex error fn (later) incase any of the mutex fns fail and to save lines
-- handle thread error fn (later) similar to handle mutex error


